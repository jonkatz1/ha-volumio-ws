{
  "name": "ha-volumio-ws-panel",
  "version": "0.1.0",
  "private": true,
  "description": "Volumio panel frontend for Home Assistant",
  "type": "module",
  "scripts": {
    "sync-version": "node scripts/sync-version.js",
    "postinstall": "npm run sync-version",
    "prebuild": "npm run sync-version",
    "build": "rollup -c",
    "prebuild:standalone": "npm run sync-version",
    "build:standalone": "rollup -c rollup.standalone.js",
    "build:all": "npm run build && npm run build:standalone",
    "watch": "rollup -c --watch"
  },
  "dependencies": {
    "@mdi/js": "7.4.47",
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.0",
    "@rollup/plugin-terser": "1.0.0",
    "rollup": "^4.28.0"
  }
}
